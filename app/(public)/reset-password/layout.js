export const metadata = {
  title: 'Set New Password',
  description: 'Create a new secure password for your NeoRoutine account.',
  robots: { index: false, follow: false },
};

export default function ResetPasswordLayout({ children }) {
  return children;
}
